# Project Gesture Recognition (Virtual Mouse)

This project aims to create a virtual mouse, through the use of `mediapipe`'s API, which can check for the hand's landmark position, and moving the user's mouse.
